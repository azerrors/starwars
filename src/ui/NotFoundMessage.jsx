function NotFoundMessage({ children }) {
  return (
    <div className="mt-56 text-center text-2xl font-semibold  uppercase text-red-500">
      {children}
    </div>
  );
}

export default NotFoundMessage;
